# Copia questo file in etl/.env e compila i valori reali
# NON committare etl/.env — è già in .gitignore

# URL di connessione per l'utente ETL (write)
# Formato: postgresql://utente:password@host:porta/database
ETL_DATABASE_URL=postgresql://hikesmap_etl:CAMBIA_QUESTO@localhost:5432/hikesmap
